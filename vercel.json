{
  "installCommand": "pnpm install --force --no-frozen-lockfile",
  "buildCommand": "pnpm run build",
  "framework": "nextjs",
  "functions": {
    "src/app/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "env": {
    "NODE_ENV": "production"
  },
  "cleanUrls": true,
  "regions": ["gru1"]
}