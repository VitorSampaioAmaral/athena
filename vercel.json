{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "buildCommand": "pnpm install --force && pnpm run build",
  "installCommand": "pnpm install --force --no-frozen-lockfile"
}