{
  "buildCommand": "chmod +x pre-build.sh && ./pre-build.sh",
  "installCommand": "pnpm install --force --prod=false",
  "framework": "nextjs",
  "functions": {
    "src/app/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "env": {
    "NODE_ENV": "production"
  },
  "cleanUrls": true,
  "regions": ["gru1"]
}